# Docker Compose override for CI/CD environments
# Removes port mappings that might conflict in GitHub Actions

services:
  # Keep app port for testing
  app:
    ports:
      - "8000:8000"
  
  # Remove database ports to avoid conflicts
  postgres:
    ports: []
  
  redis:
    ports: []
  
  adminer:
    ports: []