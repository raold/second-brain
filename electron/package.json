{
  "name": "voice-llm-electron-demo",
  "version": "1.0.0",
  "main": "main.js",
  "description": "Electron voice assistant demo with LLM and TTS integration.",
  "scripts": {
    "test": "jest",
    "test:unit": "jest --testPathIgnorePatterns=e2e",
    "test:e2e": "jest --config jest.e2e.config.js",
    "e2e": "playwright test",
    "lint": "npx eslint . --ext .js,.jsx,.ts,.tsx",
    "build": "electron-builder"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "electron-builder": "^24.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^30.0.4",
    "playwright": "^1.44.0"
  }
}
